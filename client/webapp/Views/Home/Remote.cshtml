@model RemoteViewModel
<script>

fetch("@Model.InforURL?token=@Model.token", {
    method: "GET",
    }).then(async res => {
        var sessionInfor = await (res).json();
        app.remoteToken = "@Model.token"; 
        app.SetupSession(sessionInfor);
        WebrtcConnect();
        app.connectServer();
    }
);
</script>